<% if current_user.present? %>

    <h4>喜欢的分享>

    <% my_favorite_gists.each do |gist| %>
        <div class="muted">
          <i class="icon-pencil"></i>&nbsp;
          <%= link_to gist.title, gist %>
          <br/>
          <small>
            创建于 <%= time_ago_in_words(gist.created_at) + ' ago' %>
          </small>
        </div>
    <% end %>
    <small>
      <%= link_to "更多...", user_path(current_user) %>
    </small>
    <hr/>

<% end %>
